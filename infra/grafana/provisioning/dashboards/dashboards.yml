apiVersion: 1

providers:
  - name: keso-ai
    orgId: 1
    folder: "KESO AI"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
