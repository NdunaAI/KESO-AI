NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# TODO: remove once Keycloak OIDC login is wired up (docs/03-api-specification.md #3.4);
# this is only a placeholder to let the chat UI call an already-authenticated API Gateway in dev.
NEXT_PUBLIC_DEV_BEARER_TOKEN=
